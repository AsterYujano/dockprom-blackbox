modules:
      https_2xx:
        prober: http
        timeout: 10s
        http:
          method: GET
          no_follow_redirects: false
          fail_if_ssl: false
          fail_if_not_ssl: false # true
